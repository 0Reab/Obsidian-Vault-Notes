Packet management system for installing downloading and updating stuff on your system such as binaries or packages and dependencies.
```shell
sudo apt update
sudo apt upgrade
# update your debian system such as kali
```
[[sudo]]